<?php

/**
 * @file
 * Giphy field module adds a field for Giphy images.
 */

/**
 * Implements hook_theme().
 */
function giphy_field_theme($existing, $type, $theme, $path) {
  return array(
    'giphy_field' => array(
      'variables' => array(
        'giphy_url' => NULL,
        'giphy_id' => NULL,
        'settings' => array(),
      ),
    ),
  );
}
